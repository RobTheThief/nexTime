import { DefaultTheme } from "@react-navigation/native";

const nexTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "rgb(255, 161, 131)",
    text: 'rgb(204, 102, 0)'
  },
};

export default nexTheme;
